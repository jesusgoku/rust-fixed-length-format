# Fixed Length Format - Files Builder

Build files with fixed length format.

## TODO

- Connect different sources (API, iterable)
- Connect different targets (stdout, file, etc)
- Move to library

## Run

```bash
cargo run
```